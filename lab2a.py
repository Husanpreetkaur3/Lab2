#!/usr/bin/env python3
  # This tells the terminal to use Python to run the script

name = 'Jon'  # This stores the name Jon in the variable 'name'
age = 20  # This stores the age 20 in the variable 'age'
print('Hi ' + name + ', you are ' + str(age) + ' years old.')  # This prints a message combining name and age
