#!/usr/bin/env python3

import sys  # Import the sys module to access command-line arguments

print(sys.argv)  # Print the list of command-line arguments
print('Number of arguments:', len(sys.argv))  # Print the number of arguments
